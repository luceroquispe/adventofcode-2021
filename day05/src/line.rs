use std::str::FromStr;

use crate::common::{Coordinate, Line};
// use crate::line;

#[test]
fn test_coordinates_to_line() {
    unimplemented!();
    // Test case 1
    let input = vec![
        [Coordinate { x: 0, y: 9 }, Coordinate { x: 5, y: 9 }],
        [Coordinate { x: 8, y: 0 }, Coordinate { x: 0, y: 8 }]
    ];
}